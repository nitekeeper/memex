[project]
name = "memex"
version = "2.1.0"
description = "Memex Core: structured memory and shared knowledge runtime for AI agents."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
