.ai/memex.db
__pycache__/
*.pyc
.pytest_cache/
.worktrees/
.venv/
uv.lock

# Superpowers brainstorm visual-companion session artifacts (local-only).
# Recreated on demand by the brainstorming skill; never tracked.
.superpowers/

# Memex dist bundles — manifest tracked separately
dist/v*/**
!dist/v*/manifest.json
