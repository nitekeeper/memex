.ai/memex.db
__pycache__/
*.pyc
.pytest_cache/
.worktrees/
.venv/
uv.lock

# Superpowers brainstorm visual-companion session artifacts (local-only).
# Recreated on demand by the brainstorming skill; never tracked.
.superpowers/

# Memex dist bundles — manifest tracked separately
dist/v*/**
!dist/v*/manifest.json

# Process artifacts — bodies are recoverable via git history pre-memex#22; canonical going forward is Memex (memex:run capture)
docs/plans/
docs/specs/
docs/superpowers/

# Loom agent-chat comms harness + dogfood-capture scaffolding (memex#27).
# Local-only helpers; the durable outputs live in the Brain + the GH issue.
.loom/
