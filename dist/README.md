# dist/

Released artifacts. Cut deliberately by the release skill, not auto-promoted.

Each release includes:
- All skill files in `skills/`
- A `MANIFEST.md` declaring version, contents, and what's included
- A copy of the format specs from `docs/`

No releases yet. First will be v0.1 after dogfood validation. See `ROADMAP.md`.
